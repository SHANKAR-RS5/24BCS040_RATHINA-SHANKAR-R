<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Day 12</title>
</head>
<body>
    <script>
        //set->can't accept duplicates , to unique identify the value's
        //create a set 
        let email=new Set([]);
        email.add("java@gmail.com");
        email.add("python@gmail.com");
        email.add("js@gmail.com");
        console.log(email);
     email.add("js@gmail.com");
        console.log(email);
         email.delete("js@gmail.com");
        console.log(email);
       for (let mail of email){
        console.log(mail)

       }
       email.forEach(x =>{
        console.log(x)
       })

       let adminpermission=new Set(["write","read","delete"]);
       let userpermission=new Set(["read"]);

       function haspermission(role,permission){
        return role.has(permission);

       }
       console.log(haspermission(adminpermission,"delete"));
       console.log(haspermission(userpermission,"delete"));

       //map
       let user1={name:"Rajish"}
       let user2={name:"Naveen"}
       let map=new Map();
       map.set(user1,"welcome");
       map.set(user2,"Hai Naveen");
       console.log(map);
       map.delete(user2)
       console.log(map)
       for (let message of map.values()){
        console.log(message)
       }
  </script>
</body>
</html>
